# Bug 时间线与修复台账

建立日期：2026-06-14  
适用项目：沐辰科技二手机 MIS 管理系统  
用途：只统计使用中遇到的 bug，包括发现时间、问题现象、复现方式、影响范围和当前状态。本文档不用于直接改代码。

## 1. 使用规则

- 每发现一个 bug，新增一条记录，不覆盖历史描述。
- 复现步骤要写成可执行动作，避免只写“打不开”“不对”。
- 影响范围要说明页面、角色、业务流程和数据后果。
- 涉及数据错误、收款、库存扣减、权限绕过的 bug 优先标记为高优先级。
- 如果后续已经修复，只记录修复状态、验证方式和验证结果，不在本文档展开代码修改过程。
- 如果一个现象最后确认不是 bug，应标记为 `非缺陷/按设计如此`，保留判断原因。

## 2. 状态口径

| 状态 | 含义 |
| --- | --- |
| 待确认 | 已发现现象，但还没有稳定复现或定位影响范围 |
| 待修复 | 已确认是 bug，等待开发处理 |
| 修复中 | 已开始修改代码或数据处理方案 |
| 待验证 | 已完成修复，等待业务场景验证 |
| 已关闭 | 已验证通过，无需继续处理 |
| 暂缓 | 暂不修复，需记录原因和后续触发条件 |
| 非缺陷 | 经确认属于设计预期、操作误解或外部环境问题 |

## 3. 优先级口径

| 优先级 | 判断标准 | 处理建议 |
| --- | --- | --- |
| P0 | 导致系统无法启动、核心业务无法使用、数据丢失或金额/库存严重错误 | 立即处理，修复前避免继续录入相关数据 |
| P1 | 影响维修、库存、会员、财务等主流程，但有临时绕行办法 | 优先进入下一轮修复 |
| P2 | 影响局部功能、显示、筛选、校验或体验，不阻断主流程 | 排期处理 |
| P3 | 文案、样式、小交互或低频边缘问题 | 顺手修复或集中处理 |

## 4. Bug 时间线

| ID | 发现日期 | 发现人 | 模块/页面 | 问题现象 | 复现步骤 | 影响范围 | 优先级 | 状态 | 验证结果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-20260614-001 | 2026-06-14 | 用户反馈 | 新建订单/工单详情 | 新建订单时，未填写的设备字段生成后显示为 `??` | 新建订单时部分字段不填写，生成订单后进入工单详情查看设备信息 | 空字段被异常占位，影响订单信息准确性和后续查看 | P2 | 已关闭 | 已修复。前端详情展示组件已移除 `??` 空值兜底；`vitest`、`tsc -b`、生产构建通过。 |
| BUG-20260614-002 | 2026-06-14 | 用户反馈 | 工单详情/故障与维修详情 | 不管添加什么故障，最终展示的故障名称都是“屏幕总成更换” | 在维修与故障区域添加不同故障后查看明细展示 | 维修项目无法真实反映录入内容，影响报价、维修记录和对账 | P1 | 已关闭 | 已修复。详情表优先展示接口返回的真实 `repair_items`，新增项目会传递 `sku_id`；构建产物已更新并确认无硬编码兜底。 |
| BUG-20260614-003 | 2026-06-14 | 用户反馈 | 新建工单/故障与维修详情 | 手动输入故障后录入订单提示“请选择故障代码”，但界面没有故障代码输入入口 | 手动填写故障名称、数量和费用后点击加入明细或提交订单 | 手动故障无法正常录入，影响新增非预设故障和真实维修场景 | P1 | 已关闭 | 已修复。手动故障不再强制选择预设故障代码；后端会自动生成 `AUTO-` 故障代码并写入 SKU 列表；测试和构建通过。 |
| BUG-20260614-004 | 2026-06-14 | 用户反馈 | 新建工单/故障与维修详情 | “配件成本”字段表述容易误导，应为对外报价中的“配件价格” | 打开添加故障表单，查看费用字段名称 | 容易把客户报价误解为内部成本，影响报价口径和业务理解 | P2 | 已关闭 | 已修复。添加故障、明细表和故障代码管理中的报价字段已统一为“配件价格”和“人工费”；构建产物已更新。 |

## 5. 详细记录模板

后续每个 bug 可以在这里补详细说明。表格里保留摘要，这里记录完整上下文。

### BUG-20260614-001

- 发现时间：2026-06-14
- 当前状态：已关闭
- 优先级：P2
- 模块/页面：新建订单、工单详情、设备信息
- 问题现象：新建订单时没有填写内容的字段，生成订单后在详情页显示为 `??`。
- 期望结果：未填写的字段应保持为空，不应自动填入 `??` 或其他占位内容。
- 实际结果：截图中设备信息里的“序列号”“机况”等未填写字段显示为 `??`。
- 复现步骤：
  1. 新建一个维修订单。
  2. 设备信息里部分字段不填写，例如序列号、机况。
  3. 生成订单后进入工单详情。
  4. 查看设备信息中未填写字段的显示内容。
- 影响判断：不阻断新建订单，但会让空字段变成异常文本，影响订单资料准确性，也可能影响后续搜索、导出或统计。
- 相关数据：截图订单号 `FX20260614-0001`，详情页中“序列号”“机况”字段显示为 `??`。
- 验证方式：重新新建一个部分字段留空的订单，确认详情页对应字段为空；历史已生成订单如存在 `??`，需单独确认是否只显示异常还是数据已经保存为 `??`。
- 验证结果：已修复。前端详情展示组件已移除 `??` 空值兜底，未填写的序列号、机况等字段显示为空；已通过 `vitest`、`tsc -b` 和生产构建验证。
- 备注：此记录只统计 bug，不涉及本次代码修改。

### BUG-20260614-002

- 发现时间：2026-06-14
- 当前状态：已关闭
- 优先级：P1
- 模块/页面：工单详情、故障与维修详情
- 问题现象：在“维修与故障”区域添加不同故障后，最终展示的故障名称仍固定为“屏幕总成更换”。
- 期望结果：用户添加什么故障，明细表就展示对应的故障名称和维修项目，不应被固定值覆盖。
- 实际结果：截图中“故障名称”列展示为“屏幕总成更换”，用户反馈无论添加什么故障最终都显示该内容，没有实现真正意义上的更新故障。
- 复现步骤：
  1. 打开一个工单详情。
  2. 在“维修与故障”区域添加一个非“屏幕总成更换”的故障。
  3. 保存或生成维修明细。
  4. 回到“故障与维修详情”表格查看“故障名称”列。
- 影响判断：会导致维修项目记录失真，进而影响报价说明、客户确认、维修履历、财务对账和后续追溯，属于主流程数据准确性问题。
- 相关数据：截图中“故障与维修详情”表格的“故障名称”列固定显示“屏幕总成更换”。
- 验证方式：分别添加多个不同故障，确认详情表中每条故障名称都与实际选择/填写内容一致；刷新页面后再次确认数据没有回退为固定文本。
- 验证结果：已修复。工单详情“故障与维修详情”优先使用接口返回的真实 `repair_items`，不再用“屏幕总成更换”作为无数据兜底；新增维修项目会提交 `sku_id` 以保留真实故障名；已通过 `vitest`、`tsc -b`、生产构建及构建产物静态检查。
- 备注：此记录只统计 bug，不涉及本次代码修改。

### BUG-20260614-003

- 发现时间：2026-06-14
- 当前状态：已关闭
- 优先级：P1
- 模块/页面：新建工单、故障与维修详情、添加故障
- 问题现象：手动输入故障名称后，系统要求必须选择故障代码才可以录入订单，但页面没有提供故障代码输入接口。
- 期望结果：手动输入新故障时，系统应能自动生成故障代码，并将该故障同步加入 SKU/故障代码列表，后续可直接选择复用。
- 实际结果：截图中已手动输入“主板维修”、数量和工时/服务费，但页面提示“请选择故障代码”，导致无法完成录入。
- 复现步骤：
  1. 进入“新建工单”页面。
  2. 在“故障与维修详情”区域点击“添加故障”。
  3. 不选择上方预设故障，手动输入一个新的故障名称，例如“主板维修”。
  4. 填写数量、配件成本或工时/服务费。
  5. 点击“加入明细”或继续提交订单。
- 影响判断：会阻断手动新增非预设故障，导致真实维修场景中无法录入不在列表内的故障；如果故障代码和 SKU 列表不同步，也会影响后续报价、库存/SKU 关联和维修项目复用。
- 相关数据：截图中提示“请选择故障代码”；手动输入故障名称为“主板维修”，工时/服务费为 `177`。
- 验证方式：手动新增一个 SKU 列表中不存在的故障，确认系统自动生成唯一故障代码、成功加入明细、订单可提交；提交后再次新建订单，确认该故障已进入 SKU/故障代码列表并可选择复用。
- 验证结果：已修复。手动输入新故障时可直接加入明细并提交订单；后端会为无 `sku_id` 的手动故障自动生成唯一 `AUTO-` 故障代码，并同步写入 SKU/故障代码列表以便后续复用；已通过新增后端回归测试、全量后端测试、前端测试、`tsc -b` 和生产构建验证。
- 备注：此记录只统计 bug，不涉及本次代码修改。

### BUG-20260614-004

- 发现时间：2026-06-14
- 当前状态：已关闭
- 优先级：P2
- 模块/页面：新建工单、故障与维修详情、添加故障、报价字段
- 问题现象：“配件成本”这个字段名称容易让人误解为内部统计记账的成本价格。
- 期望结果：该区域属于给顾客看的对外报价口径，应表达为“配件价格”和“人工费”；完整含义是订单对顾客报价中配件费用是多少钱、人工费用是多少钱。
- 实际结果：截图中字段显示为“配件成本”和“工时/服务费”，其中“配件成本”与真实业务口径不一致。
- 复现步骤：
  1. 进入“新建工单”页面。
  2. 打开“故障与维修详情”里的添加故障区域。
  3. 查看费用字段文案。
- 影响判断：不阻断录单，但会混淆对外报价与内部成本核算，可能导致员工误填内部采购成本，进而影响客户报价、利润判断和后续财务统计口径。
- 相关数据：截图中费用输入区域字段为“配件成本”和“工时/服务费”。
- 验证方式：确认添加故障表单、明细表、工单详情和相关报价展示中，对顾客报价口径统一显示为“配件价格”和“人工费”；内部成本如需统计，应使用独立字段和独立口径。
- 验证结果：已修复。添加故障表单、故障明细表和故障代码管理页已统一显示为“配件价格”和“人工费”；静态检查确认旧文案“配件成本”“工时/服务费”“配件单价”不再出现在 React 源码和新构建 bundle 中。
- 备注：此记录只统计 bug，不涉及本次代码修改。

## 6. 根因分类参考

- 前端状态：页面状态没有同步、弹窗/表单没有重置、筛选分页异常。
- 前端校验：必填、格式、金额、数量、日期等校验缺失或过严。
- API 契约：前后端字段名、枚举值、默认值或错误处理不一致。
- 后端规则：状态流转、权限、库存、收款、挂账、成本归集规则错误。
- 数据迁移：旧数据导入、默认值、历史字段兼容问题。
- 并发/重复提交：重复点击、重复创建、重复扣减库存或重复收款。
- 构建/环境：依赖、启动脚本、路径、端口、数据库位置问题。
- UI/交互：布局错位、按钮不可见、提示不清晰、移动端不可用。

## 7. 记录示例

```text
ID：BUG-20260614-002
发现日期：2026-06-14
模块/页面：维修工单详情
问题现象：点击“维修完成”后页面状态仍显示“维修中”，刷新后才更新。
复现步骤：
1. 打开一个状态为“维修中”的工单。
2. 点击“维修完成”。
3. 观察详情页顶部状态。
影响范围：不影响后端数据，但会误导前台继续操作。
优先级：P2
状态：待修复
```
