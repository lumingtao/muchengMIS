# MIS 重构前开发日志

记录日期：2026-05-22

## 1. 文档目的

当前 PWA 原型已经验证了业务方向，但基础结构仍偏薄弱，不适合作为长期生产系统继续叠加功能。本日志用于把这一轮原型中已经跑通、已经确认、值得继承的经验整理出来，作为下一次重构的需求和架构输入。

结论先行：

- 当前项目适合保留为“业务验证原型”和“MIS 字段事实参考”。
- 下一版应从业务领域模型、状态流转、数据边界、权限和审计重新设计。
- `mis_pwa` 的工作台式交互、`mis_bridge` 的 MIS 适配思路、`MIS.Core` 的真实查询字段，是本轮最有价值的资产。
- 当前代码不建议继续大规模增量扩展，尤其是写入逻辑、编码、缓存、客户资料存储和前后端职责边界需要重建。

## 2. 当前项目结构

### 2.1 根目录

- `.gitignore`：已建立基础 Git 忽略规则，用于排除缓存、虚拟环境、编译产物、`.env`、运行日志和本地数据。
- `.gitattributes` / `.editorconfig`：用于建立基础文本规范。
- `README.md`：项目入口说明。
- `MIS_REFACTOR_NOTES.md`：早期重构笔记，但当前文件内容存在中文编码损坏，仅适合作为历史参考。

### 2.2 `mis_pwa/`

Python FastAPI + 静态前端 PWA 原型，是当前主线业务验证入口。

已验证能力：

- 登录和本地角色权限。
- 总览、库存、回收入库、销售出库、维修单、客户查询、客户结账、经营报表、扫码查 IMEI 等工作台模块。
- Mock 数据模式和 Bridge 真实模式的 provider 抽象。
- 导出 CSV / XLSX、完整备份、操作日志。
- 回收入库表单分区、快捷输入、客户信息录入、客户查询入口。

当前局限：

- 业务规则分散在 API 层、provider、前端表单中。
- 客户资料使用本地 JSON 文件，不应作为生产主数据方案。
- 前端逐步堆叠后复杂度上升，缺少统一组件和状态管理边界。
- Service Worker 容易造成开发预览缓存不一致。

### 2.3 `mis_bridge/`

C# HTTP bridge，用于通过 `MIS.API.dll` 接入真实 MIS。

已验证价值：

- 可以作为 PWA 与真实 MIS 之间的隔离层。
- 可以集中封装查询、写入、字段白名单、schema 探测。
- 可以避免前端直接传 SQL。

当前局限：

- 写入 SQL 仍以字符串构造为主，长期需要更严格的命令层、转义策略和测试。
- schema 迁移目前只生成建议，不能直接作为生产迁移工具。
- Bridge 更适合做“MIS 适配器”，不适合作为所有业务规则的大脑。

### 2.4 `MIS.Core/`

C# 共享/参考层，保留了最重要的真实 MIS 查询事实。

已确认核心表：

- `沐辰科技二手机`：二手机库存、回收、销售、结账相关主表。
- `维修账单`：维修业务主表。

`MIS.Core/MisQueryService.cs` 是下一版梳理字段和报表口径的重要依据。

## 3. 已确认业务流程

### 3.1 回收入库

业务目的：统一新增二手机库存，记录来源、成本、设备基础信息和结账状态。

核心字段：

- 设备：机型、内存、电池效率、颜色、国家、版本、保修、机况、IMEI、序列号。
- 回收：卖家、回收人、回收价格、备注。
- 状态：默认进入 `在库`。
- 结账：`是否结账` 可为 `未结` 或 `已结`。
- 客户：客户姓名、联系方式、客户类别、店铺名称、地址、客户标签。

已得到的交互经验：

- 表单必须按业务分组，不能把所有字段平铺在一个大表单里。
- 机型、内存、颜色、国家、版本、保修、回收人等字段需要快捷输入。
- 机型快捷输入非常关键，例如输入 `15` 应展示 `iPhone 15`、`iPhone 15 Pro`、`iPhone 15 Pro Max` 等候选。
- 客户信息不应做成单独的“通讯录卡片”插在入库流程中，更适合在录入字段中直接快捷选择并自动带出资料。

### 3.2 库存管理

业务目的：查询、筛选、编辑在库设备。

现有查询口径：

- 来源表：`沐辰科技二手机`
- 条件：`状态 = '在库'`
- 主要展示字段：机型、内存、电池效率、颜色、国家、版本、保修、机况、IMEI、序列号。

关键规则：

- IMEI 是设备第一唯一键。
- 序列号是辅助展示和校验字段。
- 库存编辑必须保留操作日志。

### 3.3 销售出库

业务目的：把在库设备销售给买家，并进入已出库或待结账流程。

核心字段：

- IMEI
- 买家
- 销售人
- 销售时间
- 销售价格
- 是否结账

关键状态：

- 设备状态更新为 `已出`。
- `是否结账 = '未结'` 的销售记录进入客户结账。
- `是否结账 = '已结'` 的销售记录不进入欠款汇总。

已确认查询口径：

- 来源表：`沐辰科技二手机`
- 条件：`状态 = '已出' AND 是否结账 = '未结'`
- 主要字段：销售时间、买家、销售人、机型、颜色、IMEI、序列号、销售价格、回收人。

### 3.4 维修单

业务目的：记录客户维修业务，并支持挂账、流转、结账。

核心字段：

- 时间
- 顾客
- 机型
- 解决方案
- 报价
- 付款方式
- 状态
- 是否结账
- 备注

建议状态流：

1. 接机
2. 维修中
3. 完成
4. 取机
5. 已结账

已确认历史口径：

- 来源表：`维修账单`
- 原有挂账口径主要依赖 `付款方式 = '同行挂账'`。
- 下一版应明确区分“付款方式”和“结账状态”，避免把支付方式当作欠款状态。

### 3.5 客户结账

业务目的：财务按客户维度汇总未结销售和维修挂账，支持勾选单项或批量确认。

当前原型思路：

- 输入客户名称。
- 查询该客户未结销售。
- 查询该客户未结或挂账维修。
- 汇总金额。
- 确认后把相关记录更新为已结。

下一版需要补强：

- 结账对象必须有稳定 ID，不能只依赖客户姓名字符串。
- 销售结账和维修结账要有独立明细。
- 结账动作要生成不可随意覆盖的结账流水。
- 已结记录仍应能追溯到原业务单据。

### 3.6 客户资料与客户查询

业务目的：为销售、回收、维修、结账、VIP、商家折扣提供统一客户主数据。

当前原型已验证字段：

- 客户姓名
- 联系方式
- 客户类别：个人客户、商家客户
- 店铺名称
- 地址
- 客户标签
- 备注

交互经验：

- 个人客户只需要轻量录入联系方式。
- 商家客户需要店铺名称、地址等扩展信息。
- 客户查询应作为独立模块出现在左侧导航中。
- 回收入库、销售、维修等表单中的客户字段应支持快捷输入并自动带出资料。
- 客户查询需要能通过姓名或联系方式查到客户资料、交易记录、标签、商家信息。

当前局限：

- 原型中客户资料保存在 `mis_pwa/data/customers.json`，只能作为 demo。
- 客户交易记录目前主要从未结销售/维修推导，不是完整历史流水。

### 3.7 经营报表

业务目的：让管理者看到库存成本、未结销售、客户欠款、维修挂账、回收成本和库存状态分布。

已验证报表方向：

- 库存成本
- 销售未结
- 客户欠款
- 维修挂账
- 回收成本
- 库存状态分布

已确认 MIS 汇总口径参考：

- 基于 `沐辰科技二手机`
- 条件常见为 `是否结账 = '未结'`
- 按 `回收人` 统计成本
- 特定回收人历史口径包括 `蒋国权`、`陈秀芳`、`王新淇1`
- 状态维度包括 `在库`、`已出` 以及其他非正常/待处理状态

下一版建议：

- 报表层只读。
- 报表口径要显式命名，并写入文档。
- 每张报表要能追溯明细。

### 3.8 手机扫码查 IMEI

业务目的：手机端快速扫 IMEI 查询设备状态、交易、维修、结账关联记录。

已验证方向：

- 浏览器扫码优先。
- 扫码失败时提供手动输入。
- 查询结果展示设备详情、状态、销售/维修/结账关联信息。
- 根据权限显示可执行动作。

下一版重点：

- 手机端首屏应围绕扫码和快速动作设计。
- IMEI 查询接口应稳定、快速、只返回权限允许看到的字段。

## 4. 数据结构整理

### 4.1 设备实体 Device

建议领域字段：

- `imei`：主唯一键。
- `serial`：序列号。
- `model`：机型。
- `memory`：内存。
- `battery`：电池效率。
- `color`：颜色。
- `country`：国家。
- `version`：版本。
- `warranty`：保修。
- `condition`：机况。
- `status`：设备状态。
- `seller`：卖家。
- `recycler`：回收人。
- `recycle_price`：回收价格。
- `recycle_time`：回收时间。
- `buyer`：买家。
- `salesperson`：销售人。
- `sale_price`：销售价格。
- `sale_time`：销售时间。
- `settlement_status`：是否结账。
- `remark`：备注。

对应现有 MIS 字段：

- `机型`、`内存`、`电池效率`、`颜色`、`国家`、`版本`、`保修`、`机况`、`IMEI`、`序列号`
- `状态`、`卖家`、`回收人`、`回收价格`、`回收时间`
- `买家`、`销售人`、`销售价格`、`销售时间`
- `是否结账`、`备注`

### 4.2 维修单实体 RepairBill

建议领域字段：

- `repair_id`：维修单 ID。
- `created_at`：时间。
- `customer_id`：客户 ID。
- `customer_name`：顾客。
- `model`：机型。
- `solution`：解决方案。
- `quote`：报价。
- `payment_method`：付款方式。
- `status`：维修状态。
- `settlement_status`：结账状态。
- `remark`：备注。

对应现有 MIS 字段：

- `时间`
- `顾客`
- `机型`
- `解决方案`
- `报价`
- `付款方式`
- `状态`
- `是否结账`
- `备注`

### 4.3 客户实体 Customer

下一版应作为正式主数据，而不是临时 JSON。

建议字段：

- `customer_id`
- `name`
- `phone`
- `wechat`
- `category`：个人客户、商家客户。
- `shop_name`
- `address`
- `tags`
- `vip_level`
- `discount_policy`
- `created_at`
- `updated_at`
- `remark`

关键建议：

- 业务单据应引用 `customer_id`。
- 客户姓名和联系方式可作为搜索字段，但不应作为唯一业务主键。
- 商家折扣、VIP 后续都应挂在客户主数据或客户等级策略上。

### 4.4 结账实体 Settlement

建议下一版新增清晰结账模型。

建议字段：

- `settlement_id`
- `customer_id`
- `settlement_time`
- `operator`
- `total_amount`
- `status`
- `remark`

结账明细：

- `settlement_item_id`
- `settlement_id`
- `source_type`：sale 或 repair。
- `source_id`：销售设备 IMEI 或维修单 ID。
- `amount`
- `previous_status`
- `new_status`

### 4.5 操作日志 OperationLog

当前原型已验证本地日志价值。

建议字段：

- `time`
- `user`
- `role`
- `action`
- `target_type`
- `target_id`
- `imei`
- `customer_id`
- `request_summary`
- `result`
- `error`

下一版建议：

- 开发期可写本地文件。
- 生产期应进入数据库或不可轻易修改的审计存储。

## 5. 权限模型

当前原型角色：

- 管理员：全权限。
- 员工：入库、库存、销售、维修、扫码查询、导出、客户查询。
- 财务：结账、经营报表、扫码查询、导出、备份、客户查询。

下一版建议：

- 权限不要只绑定页面菜单，要绑定后端业务动作。
- 每个写操作必须后端再次校验权限。
- 审计日志记录操作者、角色和结果。
- 后续可加入用户管理、密码策略、角色配置和操作二次确认。

## 6. 架构经验

### 6.1 值得保留的方向

- PWA 工作台适合作为主界面。
- Bridge 适合作为真实 MIS 的适配层。
- Mock provider 对开发演示和测试很有价值。
- 左侧模块导航符合实际业务操作习惯。
- 回收入库、销售出库、维修、结账、客户查询、报表、扫码查询是合理的一期业务边界。
- API 不应允许前端传原始 SQL。
- 写入前摘要确认、成功提示、失败原因展示是必要风控。

### 6.2 需要重建的基础

- 领域模型：先定义设备、客户、销售、维修、结账、日志，再写页面。
- 状态机：设备状态、维修状态、结账状态必须明确。
- 数据访问：统一命令层，集中处理字段白名单、转义、错误和影响行数。
- 编码：全项目强制 UTF-8，避免中文字段和文案损坏。
- 缓存：开发阶段先弱化 PWA 缓存，生产再启用明确版本策略。
- 客户主数据：不能继续依赖本地 JSON。
- 测试：权限、IMEI 唯一、状态流转、结账批量更新、报表聚合必须自动化。

## 7. 下一版推荐架构

建议分层：

1. 前端工作台
   - 负责交互、表单、扫码、提示、导出触发。
   - 不包含核心业务判定。

2. PWA API / 应用服务层
   - 负责登录、权限、业务用例编排、表单校验、操作日志。
   - 提供稳定业务接口，例如 `CreatePurchase`、`SellDevice`、`CreateRepair`、`SettleCustomer`。

3. 领域层
   - 定义实体、状态、规则、DTO。
   - 与 MIS 字段名解耦。

4. MIS Bridge / 适配层
   - 只负责把业务命令映射到 `MIS.API.dll` 查询和写入。
   - 维护字段映射、schema 探测、错误转换。

5. 数据层
   - 真实 MIS 表。
   - 客户主数据和结账流水如 MIS 不支持，应明确独立存储方案。

## 8. 下一版 MVP 顺序建议

第一步：需求与领域建模

- 明确设备状态全集。
- 明确维修状态全集。
- 明确结账状态和结账流水。
- 明确客户主数据存在哪里。
- 明确哪些字段来自真实 MIS，哪些字段是新系统补充。

第二步：只读数据打底

- 设备查询。
- 库存列表。
- IMEI 查询。
- 维修列表。
- 客户查询。
- 报表只读口径。

第三步：写入闭环

- 回收入库。
- 销售出库。
- 维修单创建和状态流转。
- 客户结账。
- 操作日志。

第四步：移动端效率

- 手机扫码。
- 快速入库。
- 快速出库。
- 客户快捷选择。

第五步：经营增强

- VIP。
- 商家折扣。
- 更完整的客户交易历史。
- 更多报表和导出。

## 9. 重构时必须避免的问题

- 不要先做页面，再把业务规则散落到表单里。
- 不要把客户姓名当作唯一主键。
- 不要把付款方式等同于结账状态。
- 不要让前端直接传 SQL。
- 不要在没有测试库和备份策略时直接写真实库。
- 不要忽视中文编码、浏览器缓存和 PWA Service Worker。
- 不要把 mock 数据结构和真实 MIS 字段随意混在一起。

## 10. 当前原型可作为参考的文件

- `MIS.Core/MisQueryService.cs`：真实 MIS 查询口径参考。
- `mis_bridge/MisBridgeServer.cs`：Bridge 适配和 schema 探测思路参考。
- `mis_pwa/backend/app.py`：业务 API 草图参考。
- `mis_pwa/backend/providers.py`：Mock/Bridge provider 抽象参考。
- `mis_pwa/static/index.html`：工作台模块布局参考。
- `mis_pwa/static/app.js`：前端业务交互、快捷输入、客户查询参考。
- `mis_pwa/static/styles.css`：表单分区和工作台视觉方向参考。
- `mis_pwa/data/mock/views.json`：演示数据和业务样例参考。

## 11. 重构验收标准建议

下一版基础完成时，至少应满足：

- 可以用测试数据完整跑通回收入库、销售出库、维修单、客户结账。
- 每个写操作都有权限校验、表单校验、摘要确认、操作日志和错误提示。
- IMEI 唯一性由后端保证。
- 客户主数据有稳定 ID，并能在各业务表单中快捷选择。
- 报表数据可以追溯到明细。
- 中文源码、页面、JSON、导出文件不乱码。
- 开发环境刷新页面不会被旧缓存干扰。
- Mock 模式和真实 Bridge 模式共用同一套业务接口契约。

