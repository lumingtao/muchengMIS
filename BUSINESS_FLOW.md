# 当前业务流动说明书（维修业务重点）

本文档记录当前 `mis_mvp` 的业务流动事实，用于后续开发、测试、培训、交接和真实 MIS 接入。文档主线以新机器生命周期模型为准：所有新业务围绕 `machines.machine_id` 展开，维修、回收、库存、销售、支付、客户和时间线都关联到机器档案。

旧 `devices`、`repairs` 表属于旧 MVP 兼容数据，仍可用于历史导入、旧接口和对照测试，但不作为后续业务主线。

## 01 当前业务全景图

当前系统是一套围绕“机器生命周期”的轻量 MIS MVP。业务从机器到店或回收入库开始，经过检测、报价、维修、入库、销售、交付、收付款、结账，最终沉淀为机器时间线、财务流水、客户记录和报表。

统一业务对象：

- `machines` 是新业务主表，记录机器编号、IMEI、序列号、机型、内存、颜色、机况、业务来源、当前状态、客户、备注、创建人和关闭时间。
- `repair_orders`、`recycle_orders`、`inventory_items`、`sales_orders`、`payments` 都通过 `machine_id` 或业务单据 ID 与机器关联。
- `customers` 记录客户主数据，维修、回收、销售、结账都可以复用客户档案。
- `machine_events` 记录机器生命周期事件，`machine_notes` 记录追加备注。
- `operation_logs` 记录后端业务动作审计。

统一业务线：

- 维修：客户机器到店，完成开单、检测、报价、维修、交付、收款结单。
- 回收：客户机器回收，完成开单、验机、报价、付款、入库。
- 库存：回收入库后的机器形成可销售库存，带成本和销售定价。
- 销售：库存机器销售给客户，形成销售单和后续收入流水。
- 财务：维修和销售登记收入，回收登记支出，客户可按未结明细批量结账。
- 报表：按库存、机器状态、收支方向、未结业务明细汇总经营情况。

统一角色边界：

- 管理员：拥有所有机器、维修、回收、库存、销售、支付、结账、报表和审计权限。
- 员工：负责机器建档、维修开单与推进、回收开单与推进、库存读取、销售开单、客户查询。
- 财务：负责支付流水、客户结账、报表、审计读取；不能创建或推进维修单。

## 02 机器生命周期总览

机器生命周期是当前系统的核心抽象。无论机器来自维修、回收还是已有档案，业务系统都应优先识别或创建一条机器档案，再围绕该机器追加单据、状态和事件。

机器关键字段：

- `machine_id`：系统内部稳定主键。
- `machine_no`：机器编号；有 IMEI 时生成 `MC-后六位-随机码`，无 IMEI 时生成 `TMP-随机码`。
- `imei`：优先唯一键；非空时不能重复。
- `serial`：序列号，用于辅助识别和校验。
- `model`、`memory`、`color`、`condition`：机器基础信息。
- `source_type`：业务来源，当前主要为 `维修`、`回收`、`销售`。
- `current_status`：机器当前状态。
- `customer_id`：关联客户。
- `created_by`、`created_at`、`updated_at`、`closed_at`：创建、更新和关闭追踪。

机器状态全集：

- `到店`：机器刚建档，还未进入具体业务处理。
- `检测中`：维修或回收已开单，正在检测。
- `已报价`：检测完成，并记录了报价。
- `维修中`：维修单已记录维修项目，进入实际维修。
- `待交付`：维修完成，等待交付检测或客户取机。
- `已交付`：维修业务已交付，等待财务收款结单。
- `已回收`：保留给回收业务中间状态。
- `回收库存`：回收付款入库后成为库存。
- `待销售`：保留给库存定价后待销售状态。
- `已售出`：库存机器已销售。
- `已结单`：业务和财务闭环完成。

生命周期追踪规则：

- 机器创建时写入“机器到店建档”事件。
- 维修开单、检测报价、维修改价、维修项目、交付检测、收款流水都应写入机器时间线。
- 回收开单、验机报价、回收改价、回收入库、回收付款都应写入机器时间线。
- 销售开单、销售收款应写入机器时间线。
- 手工备注使用 `machine_notes` 追加保存，不覆盖历史备注。

## 03 维修业务详细流程（重点）

维修业务是当前系统最重要的业务闭环。它负责把客户送修机器从到店开单推进到检测、报价、维修、交付和收款结单，并要求每一步都可追踪、可校验、可回放。

### 3.1 到店建档 / 维修开单

入口：

- 前端“维修开单”页面。
- 后端 `POST /api/repair-orders`。

操作人：

- 员工。
- 管理员。

输入：

- 可选择已有 `machine_id`。
- 若没有已有机器，则录入机器信息：IMEI、序列号、机型、内存、颜色、机况。
- 客户信息：客户姓名、联系电话等。
- 故障描述：客户报修问题、外观和初步现象。

系统动作：

- 如果传入 `machine_id`，系统查找已有机器档案；不存在则拒绝开单。
- 如果未传入 `machine_id`，系统创建机器档案；IMEI 非空时必须唯一，无 IMEI 时生成 `TMP-` 临时机器编号。
- 系统创建 `repair_orders` 记录，初始维修单状态为 `已开单`。
- 系统把机器状态更新为 `检测中`，业务来源更新为 `维修`。
- 系统写入机器时间线事件“维修开单”。
- 系统写入操作审计 `repair_order:create`。

输出：

- 返回维修单详情，包括维修单、机器、客户、项目、付款、事件和可用动作。
- 当前可用动作通常为开始检测和作废。

业务意义：

- 将“客户送修”变成系统可管理的维修单。
- 先确立机器档案，后续所有检测、报价、维修、交付和收款都能追溯到同一台机器。

注意事项：

- 当前代码中维修单创建后，维修单状态是 `已开单`，机器状态已进入 `检测中`。文档和培训中要区分“维修单流程状态”和“机器当前状态”。
- 客户可在开单时新建或复用；后续客户结账依赖稳定 `customer_id`，不应只依赖客户姓名。

### 3.2 开始检测

入口：

- 前端“维修流程推进”中的“开始检测”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/status`，目标状态为 `检测中`。

前置状态：

- 维修单必须是 `已开单`。

系统动作：

- 维修单状态从 `已开单` 变为 `检测中`。
- 机器状态保持或进入 `检测中`。
- 写入机器时间线事件“维修检测中”。
- 写入操作审计 `repair_order:status`。

业务意义：

- 确认机器已经进入检测环节。
- 防止维修单从刚开单直接进入报价、交付或收款。

异常规则：

- 已结单或已作废维修单不能继续推进。
- 非 `已开单` 状态不能再次进入开始检测。

### 3.3 检测报价

入口：

- 前端“检测报价”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/quote`。

前置状态：

- 维修单必须是 `检测中`。

输入：

- `diagnosis`：检测结论，必填。
- `quoted_amount`：客户报价，必须大于或等于 0。

系统动作：

- 写入维修单检测结论和报价。
- 维修单状态进入 `已报价`。
- 机器状态进入 `已报价`。
- 写入机器时间线事件“检测报价”，内容包含检测结论和报价金额。
- 写入操作审计 `repair_order:quote`。

业务意义：

- 把检测结果和客户报价固化为后续维修、改价、收款、结账的金额基础。
- 让客户确认维修预期，避免维修完成后再产生价格争议。

异常规则：

- 未开始检测不能报价。
- 已作废或已结单维修单不能报价。
- 检测结论不能为空。

### 3.4 改价

入口：

- 机器时间线详情里的改价表单。
- 后端 `POST /api/repair-orders/{repair_order_id}/price`。

输入：

- `quoted_amount`：新报价，必须大于或等于 0。
- `remark`：改价原因或沟通记录。

系统动作：

- 更新维修单报价金额。
- 写入机器时间线事件“维修改价”，内容包含旧报价、新报价和备注。
- 写入操作审计 `repair_order:price`。

业务意义：

- 保留报价调整历史，避免口头改价不可追溯。
- 支持检测加项、客户砍价、配件价格变化等真实业务情况。

当前实现边界：

- 当前代码允许对存在的维修单改价，没有额外限制状态。
- 后续生产规范建议明确：哪些状态允许改价、是否需要管理员或财务复核、已交付后改价是否必须写明原因。

### 3.5 记录维修项目 / 配件

入口：

- 前端“记维修项目”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/items`。

前置状态：

- 维修单必须是 `已报价`。

输入：

- `item_name`：维修项目或配件名称，必填。
- `quantity`：数量，默认 1，必须大于或等于 1。
- `cost_amount`：成本金额，必须大于或等于 0。
- `charge_amount`：收费金额，必须大于或等于 0。
- `remark`：项目备注。

系统动作：

- 新增 `repair_items` 明细。
- 维修单状态从 `已报价` 进入 `处理中`。
- 机器状态进入 `维修中`。
- 写入机器时间线事件“维修项目”。
- 写入操作审计 `repair_order:item`。

业务意义：

- 把“客户报价”和“实际维修动作”拆开记录。
- 后续可按维修项目统计成本、收费、利润和责任。

异常规则：

- 未报价不能记录维修项目。
- 已作废或已结单维修单不能记录维修项目。
- 当前实现中，新增第一个维修项目后维修单进入 `处理中`；已进入 `处理中` 后不能通过同一接口继续追加项目，这是后续需要评估的业务规则。

### 3.6 标记待交付

入口：

- 前端“待交付”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/status`，目标状态为 `待交付`。

前置状态：

- 维修单必须是 `处理中`。

系统动作：

- 维修单状态进入 `待交付`。
- 机器状态进入 `待交付`。
- 写入机器时间线事件“维修待交付”。
- 写入操作审计 `repair_order:status`。

业务意义：

- 表示维修动作已经完成，等待交付检测、客户确认和取机。
- 把维修执行与交付检测分离，便于质检和客户沟通。

异常规则：

- 未进入维修处理不能标记待交付。
- 已作废或已结单维修单不能标记待交付。

### 3.7 交付检测

入口：

- 前端“交付检测”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/deliver`。

前置状态：

- 维修单必须是 `待交付`。

输入：

- `delivery_check`：交付检测结果，必填。
- `remark`：交付备注。

系统动作：

- 写入维修单交付检测结果。
- 维修单状态进入 `已交付`。
- 机器状态进入 `已交付`。
- 写入机器时间线事件“交付检测”。
- 写入操作审计 `repair_order:deliver`。

业务意义：

- 客户取机前确认功能正常。
- 作为财务收款结单前最后一个业务确认节点。

异常规则：

- 不能跳过 `待交付` 直接交付。
- 作废单不能交付。
- 结单后不能再次交付。

### 3.8 收款结单

入口：

- 前端“财务流水”页面。
- 维修流程中的“收款结单”按钮会跳转到财务流水并预填 `source_type=repair`、`source_id=repair_order_id`、`direction=收入`。
- 后端 `POST /api/payments`。

操作人：

- 财务。
- 管理员。

前置状态：

- 维修单必须是 `已交付`。

输入：

- `source_type=repair`。
- `source_id=repair_order_id`。
- `direction=收入`。
- `amount`：收款金额，必须大于 0。
- `method`：收款方式。
- `payer`：付款人。
- `payee`：收款人。
- `remark`：备注。

系统动作：

- 创建 `payments` 收入流水。
- 写入机器时间线事件“收入流水”。
- 如果该维修单累计维修收入小于报价金额，则暂不结单。
- 如果累计维修收入达到或超过报价金额，则维修单进入 `已结单`，机器进入 `已结单`，写入关闭时间。
- 写入操作审计 `payment:create`。

业务意义：

- 把维修交付和财务收款分离，符合门店“先交付确认、后收款结单”的闭环。
- 支持部分收款，直到累计收入达到报价后自动结单。

异常规则：

- 作废维修单不能收款。
- 已结单维修单不能重复收款。
- 未交付维修单不能收款。
- 维修单只能登记收入流水，不能登记支出流水。

### 3.9 作废

入口：

- 后端 `POST /api/repair-orders/{repair_order_id}/status`，目标状态为 `已作废`。
- 前端当前没有显式作废按钮，但后端规则已存在。

允许前置状态：

- `已开单`。
- `检测中`。
- `已报价`。
- `处理中`。
- `待交付`。

系统动作：

- 维修单状态进入 `已作废`。
- 写入机器时间线事件“维修作废”。
- 写入操作审计 `repair_order:status`。

业务意义：

- 客户放弃维修、误开单、无法维修、价格未谈拢等情况应保留单据和历史，而不是直接删除。

后续限制：

- 作废单不能交付。
- 作废单不能收款。
- 作废单不能继续维修。

## 04 维修状态机与异常规则

标准路径：

```text
已开单 -> 检测中 -> 已报价 -> 处理中 -> 待交付 -> 已交付 -> 已结单
```

作废路径：

```text
已开单 / 检测中 / 已报价 / 处理中 / 待交付 -> 已作废
```

状态动作矩阵：

| 维修单状态 | 允许动作 | 禁止动作 | 产生数据 | 机器状态影响 | 财务影响 |
| --- | --- | --- | --- | --- | --- |
| 已开单 | 开始检测、作废 | 报价、记维修项目、交付、收款 | 维修单、开单事件 | 机器已进入检测中 | 无 |
| 检测中 | 检测报价、作废 | 记维修项目、待交付、交付、收款 | 检测结论、报价 | 已报价 | 无 |
| 已报价 | 记维修项目、改价、作废 | 交付、收款 | 维修项目、改价事件 | 维修中 | 无 |
| 处理中 | 待交付、改价、作废 | 报价、直接交付、收款 | 状态事件 | 待交付 | 无 |
| 待交付 | 交付检测、改价、作废 | 维修项目、收款 | 交付检测 | 已交付 | 无 |
| 已交付 | 收款结单、改价 | 作废、追加维修项目、重复交付 | 收入流水 | 达到金额后已结单 | 维修收入 |
| 已结单 | 查看 | 推进、作废、追加维修项目、重复收款 | 无新增业务数据 | 已结单 | 已闭环 |
| 已作废 | 查看 | 推进、交付、收款 | 无新增业务数据 | 当前实现不额外关闭机器 | 无 |

当前已实现的硬性限制：

- 只能从 `已开单` 进入 `检测中`。
- 只能从 `检测中` 进入 `已报价`。
- 只能从 `已报价` 记录维修项目并进入 `处理中`。
- 只能从 `处理中` 进入 `待交付`。
- 只能从 `待交付` 进入 `已交付`。
- 只能在 `已交付` 后登记维修收入。
- 作废维修单不能交付或收款。
- 已结单维修单不能重复收款。
- 已结单或作废维修单不能继续业务推进。

需要进一步确认或补强的规则：

- 已进入 `处理中` 后是否允许追加多个维修项目。
- 已交付后是否允许改价，以及改价是否需要管理员或财务确认。
- 作废后机器状态是否需要进入单独的“已作废”或“待客户取回”状态。
- 维修单部分收款后是否允许继续改价。
- 客户挂账维修是否统一通过 `payments` 部分收款处理，还是继续支持客户结账单批量结算。

## 05 回收、库存、销售、财务流

### 回收业务流

标准路径：

```text
回收开单 -> 验机报价 -> 回收改价（可选） -> 付款入库 -> 形成库存 -> 登记回收支出
```

关键动作：

- 回收开单：创建或复用机器档案，创建 `recycle_orders`，机器进入 `检测中`。
- 验机报价：记录检测结果和报价，机器进入 `已报价`。
- 回收改价：记录旧报价、新报价和原因，写入时间线。
- 付款入库：记录实际付款金额，创建 `inventory_items`，机器进入 `回收库存`。
- 回收支出：系统在入库时自动创建 `payments` 支出流水。

业务意义：

- 将回收成本、库存定价、机器档案和客户来源绑定在一起。
- 后续销售时可计算库存成本与收入差额。

### 库存业务流

库存来源：

- 当前新主线库存来自回收业务的付款入库。

库存关键字段：

- `inventory_item_id`：库存项主键。
- `machine_id`：关联机器。
- `recycle_order_id`：关联回收单。
- `status`：库存状态，当前主要为 `可销售`、`已售出`。
- `cost_amount`：回收成本。
- `sale_price`：销售定价。

业务意义：

- 库存不是孤立设备列表，而是机器生命周期中的一个阶段。
- 销售必须从库存项发起，避免重复销售同一台机器。

### 销售业务流

标准路径：

```text
选择库存项 -> 销售开单 -> 库存标记已售出 -> 机器状态已售出 -> 登记销售收入 -> 结单
```

关键动作：

- 销售开单：选择 `inventory_item_id`，录入客户、销售价、销售人和备注。
- 系统创建 `sales_orders`，库存项标记 `已售出`，机器进入 `已售出`。
- 财务登记 `source_type=sale` 的收入流水后，销售单和机器进入 `已结单`。

异常规则：

- 库存项不存在不能销售。
- 已售出库存不能重复销售。

### 财务流水

统一入口：

- 前端“财务流水”页面。
- 后端 `POST /api/payments`。

流水方向：

- 维修：收入。
- 销售：收入。
- 回收：支出。

财务闭环：

- 维修收入累计达到报价后关闭维修单和机器。
- 销售收入登记后关闭销售单和机器。
- 回收支出在回收入库时登记，但不关闭机器，因为机器后续进入库存和销售。

## 06 客户与结账流

客户主数据用于连接维修、回收、销售和财务结账。客户可以在业务开单时创建，也可以通过 `customer_id` 复用已有客户。

客户字段：

- 姓名、电话、微信。
- 客户类型：个人客户、商家客户等。
- 店铺名称、地址。
- 标签、VIP 等级、折扣政策、备注。

客户结账流：

```text
选择客户 -> 查询未结销售和未结维修 -> 选择明细 -> 生成结账单 -> 生成结账明细 -> 更新业务状态
```

当前结账范围：

- 旧主线销售：`devices` 中已售出且 `settlement_status=未结` 的记录。
- 旧主线维修：`repairs` 中 `settlement_status=未结` 的记录。
- 新主线维修：`repair_orders` 中未 `已结单`、未 `已作废` 且报价大于 0 的记录。

结账结果：

- 创建 `settlements`。
- 创建 `settlement_items`，记录来源类型、来源 ID、金额、旧状态、新状态。
- 新主线维修订单结账后进入 `已结单`，关联机器进入 `已结单`。

需要进一步统一的规则：

- 新主线维修既可以通过 `payments` 单独收款结单，也可以通过客户结账批量结算；后续应明确两者关系，避免同一笔维修重复结算。
- 客户结账应优先依赖稳定 `customer_id`，客户姓名只作为搜索展示字段。

## 07 权限、审计与时间线

权限按后端动作控制，不只依赖前端菜单。

维修相关权限：

- 管理员：可创建、更新、读取维修订单，可收款、结账、看报表和审计。
- 员工：可创建、更新、读取维修订单；不能创建财务流水、不能看报表和审计。
- 财务：可读取维修订单、创建和读取支付流水、结账、看报表和审计；不能创建或推进维修订单。

审计层级：

- `machine_events`：业务时间线，面向机器生命周期查看。用于回答“这台机器经历了什么”。
- `machine_notes`：追加备注，面向人工补充说明。用于记录电话沟通、加急要求、特殊情况。
- `operation_logs`：系统操作审计，面向权限和后台追踪。用于回答“谁在什么时候做了哪个后端动作”。

维修时间线应覆盖：

- 机器到店建档。
- 维修开单。
- 开始检测。
- 检测报价。
- 维修改价。
- 维修项目。
- 待交付。
- 交付检测。
- 收入流水。

## 08 当前代码已实现 vs 待补充规则

已实现：

- 机器档案唯一 IMEI 校验和无 IMEI 临时编号。
- 维修单标准状态推进。
- 维修单禁止跳步。
- 作废维修单禁止交付和收款。
- 已结单维修单禁止重复收款。
- 维修收款必须发生在已交付之后。
- 维修收入累计达到报价后自动结单。
- 维修关键动作写入机器时间线。
- 维修相关操作写入操作审计。
- 财务角色可以收款和看报表，员工不能看财务报表。

待补充或待确认：

- 前端缺少维修作废按钮，后端已有作废能力。
- 当前新增维修项目会从 `已报价` 进入 `处理中`，但后续不能继续追加多个项目；如果业务需要多项目维修，需要调整状态规则。
- 改价没有状态限制，生产前需要明确审批和状态边界。
- 新主线维修的单独收款结单与客户批量结账需要统一去重规则。
- 作废后机器状态当前不额外关闭，需要明确机器后续如何处理。
- 维修报表需要进一步区分待检测、待维修、待交付、待收款、已结单、已作废。
- 维修单需要更完整的客户确认记录，例如报价确认时间、确认方式、联系人。

## 09 测试验收场景清单

维修主流程：

- 新建维修单后，维修单状态为 `已开单`，机器状态为 `检测中`，时间线有“维修开单”。
- 从 `已开单` 点击开始检测后，维修单进入 `检测中`。
- 从 `检测中` 报价后，维修单进入 `已报价`，机器进入 `已报价`，保存检测结论和报价金额。
- 从 `已报价` 记录维修项目后，维修单进入 `处理中`，机器进入 `维修中`，保存项目、数量、成本和收费。
- 从 `处理中` 标记待交付后，维修单和机器进入 `待交付`。
- 从 `待交付` 交付检测后，维修单和机器进入 `已交付`。
- 财务对已交付维修单登记足额收入后，维修单和机器进入 `已结单`。

维修异常流：

- 未开始检测不能报价。
- 未报价不能记录维修项目。
- 未进入待交付不能交付。
- 未交付不能收款。
- 作废维修单不能交付。
- 作废维修单不能收款。
- 已结单维修单不能重复收款。
- 已结单维修单不能追加维修项目。
- 改价必须留下“维修改价”时间线记录。
- 部分收款低于报价时不自动结单，累计达到报价后自动结单。

权限验收：

- 员工可以创建维修单、开始检测、报价、记录维修项目、待交付、交付检测。
- 员工不能创建维修收款流水。
- 员工不能查看经营报表。
- 财务可以读取维修单、创建维修收款流水、查看报表。
- 财务不能创建维修单或推进维修状态。
- 管理员可以完成所有维修、财务、报表和审计动作。

时间线验收：

- 机器时间线能串起建档、维修开单、检测报价、维修项目、交付检测、收款流水。
- 维修项目能在机器详情中看到收费和成本。
- 维修收款能在机器详情中看到金额、方向、方式和时间。
- 手工备注按追加顺序显示，不覆盖旧备注。

回收、库存、销售、财务验收：

- 回收开单后机器进入检测中。
- 验机报价后机器进入已报价。
- 回收入库后生成库存项，机器进入回收库存，并登记回收支出。
- 销售库存项后库存标记已售出，机器进入已售出。
- 销售收款后销售单和机器进入已结单。
- 报表能统计库存数量、机器状态分布和收支方向汇总。

