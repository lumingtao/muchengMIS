# 当前业务流动说明书（维修业务重点）

本文档记录当前 `mis_mvp` 的业务流动事实，用于后续开发、测试、培训、交接和真实 MIS 接入。文档主线以新机器生命周期模型为准：所有新业务围绕 `machines.machine_id` 展开，维修、回收、库存、销售、支付、客户和时间线都关联到机器档案。

旧 `devices`、`repairs` 表属于旧 MVP 兼容数据，仍可用于历史导入、旧接口和对照测试，但不作为后续业务主线。

## 01 当前业务全景图

当前系统是一套围绕“机器生命周期”的轻量 MIS MVP。业务从机器到店或回收入库开始，经过检测、报价、维修、入库、销售、交付、收付款、结账，最终沉淀为机器时间线、财务流水、客户记录和报表。

统一业务对象：

- `machines` 是新业务主表，记录机器编号、IMEI、序列号、机型、内存、颜色、机况、业务来源、当前状态、客户、备注、创建人和关闭时间。
- `repair_orders`、`recycle_orders`、`inventory_items`、`sales_orders`、`payments` 都通过 `machine_id` 或业务单据 ID 与机器关联。
- `customers` 记录客户主数据，维修、回收、销售、结账都可以复用客户档案。
- `machine_events` 记录机器生命周期事件，`machine_notes` 记录追加备注。
- `operation_logs` 记录后端业务动作审计。

统一业务线：

- 维修：客户机器到店，完成开单、检测、报价、维修、交付、收款结单。
- 回收：客户机器回收，完成开单、验机、报价、付款、入库。
- 库存：回收入库后的机器形成可销售库存，带成本和销售定价。
- 销售：库存机器销售给客户，形成销售单和后续收入流水。
- 财务：维修和销售登记收入，回收登记支出，客户可按未结明细批量结账。
- 报表：按库存、机器状态、收支方向、未结业务明细汇总经营情况。

统一角色边界：

- 管理员：拥有所有机器、维修、回收、库存、销售、支付、结账、报表和审计权限。
- 前台：负责简建单、客户接待、客户沟通、查看全部维修订单、报价确认、客户取机和现场收费。
- 工程师：负责接单后的检测、故障记录、维修方案、维修项目、维修完成和工程师结单；只能查看已指派给自己且尚在本人处理链路内的维修订单。
- 财务：负责正式支付流水、客户结账、退款/冲正、报表和审计读取；不负责检测维修动作。
- 老板/管理员：拥有全局查看、指派、异常审核、报表、审计和财务系统管理权限，可处理跨岗位争议与高风险动作。

## 02 机器生命周期总览

机器生命周期是当前系统的核心抽象。无论机器来自维修、回收还是已有档案，业务系统都应优先识别或创建一条机器档案，再围绕该机器追加单据、状态和事件。

机器关键字段：

- `machine_id`：系统内部稳定主键。
- `machine_no`：机器编号；有 IMEI 时生成 `MC-后六位-随机码`，无 IMEI 时生成 `TMP-随机码`。
- `imei`：优先唯一键；非空时不能重复。
- `serial`：序列号，用于辅助识别和校验。
- `model`、`memory`、`color`、`condition`：机器基础信息。
- `source_type`：业务来源，当前主要为 `维修`、`回收`、`销售`。
- `current_status`：机器当前状态。
- `customer_id`：关联客户。
- `created_by`、`created_at`、`updated_at`、`closed_at`：创建、更新和关闭追踪。

机器状态全集：

- `到店`：机器刚建档，还未进入具体业务处理。
- `检测中`：维修或回收已开单，正在检测。
- `已报价`：检测完成，并记录了报价。
- `维修中`：维修单已记录维修项目，进入实际维修。
- `待交付`：维修完成，等待交付检测或客户取机。
- `已交付`：维修业务已交付，等待财务收款结单。
- `已回收`：保留给回收业务中间状态。
- `回收库存`：回收付款入库后成为库存。
- `待销售`：保留给库存定价后待销售状态。
- `已售出`：库存机器已销售。
- `已结单`：业务和财务闭环完成。

生命周期追踪规则：

- 机器创建时写入“机器到店建档”事件。
- 维修开单、检测报价、维修改价、维修项目、交付检测、收款流水都应写入机器时间线。
- 回收开单、验机报价、回收改价、回收入库、回收付款都应写入机器时间线。
- 销售开单、销售收款应写入机器时间线。
- 手工备注使用 `machine_notes` 追加保存，不覆盖历史备注。

## 03 维修业务详细流程（重点）

维修业务是当前系统最重要的业务闭环。它负责把客户送修机器从到店开单推进到检测、报价、维修、交付和收款结单，并要求每一步都可追踪、可校验、可回放。

生产设计中，维修订单不再按“单个员工从头推到尾”理解，而是按公共订单池和岗位流转理解：

```text
前台简建单 -> 公共订单池 -> 指派工程师 -> 工程师检测与方案 -> SKU 报价 -> 前台沟通客户 -> 工程师维修 -> 工程师结单 -> 前台收费 -> 订单完结
```

业务状态回答“订单走到哪一步”，处理归属回答“现在谁该处理”。前台拥有全部维修订单的查看权限，便于接待、查单、沟通和收费；工程师只看到已指派给自己、且从接单到完结期间属于自己处理范围的订单；财务和老板从财务、结账、报表和审计视角查看订单。

### 3.1 到店建档 / 维修开单

入口：

- 前端“维修开单”页面。
- 后端 `POST /api/repair-orders`。

操作人：

- 前台。
- 管理员。

输入：

- 可选择已有 `machine_id`。
- 若没有已有机器，则录入机器信息：IMEI、序列号、机型、内存、颜色、机况。
- 客户信息：客户姓名、联系电话等。
- 简要故障描述：客户报修问题、外观和初步现象。

系统动作：

- 如果传入 `machine_id`，系统查找已有机器档案；不存在则拒绝开单。
- 如果未传入 `machine_id`，系统创建机器档案；IMEI 非空时必须唯一，无 IMEI 时生成 `TMP-` 临时机器编号。
- 系统创建 `repair_orders` 记录，初始维修单状态为 `已开单`。
- 生产设计中，开单后订单先进入公共订单池，处理归属为 `待指派工程师`。
- 系统把机器状态更新为 `检测中`，业务来源更新为 `维修`。
- 系统写入机器时间线事件“维修开单”。
- 系统写入操作审计 `repair_order:create`。

输出：

- 返回维修单详情，包括维修单、机器、客户、项目、付款、事件和可用动作。
- 当前可用动作通常为指派工程师、开始检测和作废。

业务意义：

- 将“客户送修”变成系统可管理的维修单。
- 先用最少信息完成前台接待，避免客户等待过长；详细故障和维修方案由工程师接单后补充。
- 先确立机器档案，后续所有检测、报价、维修、交付和收款都能追溯到同一台机器。

注意事项：

- 当前代码中维修单创建后，维修单状态是 `已开单`，机器状态已进入 `检测中`。文档和培训中要区分“维修单流程状态”和“机器当前状态”。
- 客户可在开单时新建或复用；后续客户结账依赖稳定 `customer_id`，不应只依赖客户姓名。
- 前台可查看全部维修订单，但不应代替工程师填写专业检测结论和维修方案。

### 3.1.1 公共订单池 / 指派工程师

入口：

- 前端“公共维修订单池”。
- 后端建议提供 `POST /api/repair-orders/{repair_order_id}/assign`。

操作人：

- 前台。
- 老板/管理员。

前置状态：

- 维修单已由前台简建单。
- 订单尚未完结、尚未作废。

输入：

- `engineer_user_id`：被指派工程师。
- `remark`：指派备注，例如加急、客户等待、疑似主板问题。

系统动作：

- 将维修单处理归属更新为被指派工程师。
- 订单从公共订单池进入工程师个人订单列表。
- 写入机器时间线事件“指派工程师”。
- 写入操作审计 `repair_order:assign`。

业务意义：

- 前台负责接待和调度，工程师负责专业处理。
- 工程师登录自己的账号后，只看到分配给自己的待检测、维修中、待结单订单。
- 前台仍可查看所有订单的状态、报价和处理进度，方便接待客户咨询。

异常规则：

- 已结单、已作废订单不能再次指派。
- 已指派订单如需改派，应记录原工程师、新工程师和改派原因。
- 工程师不能主动认领未指派给自己的订单，除非老板/管理员允许抢单或转派规则。

### 3.2 开始检测

入口：

- 前端“维修流程推进”中的“开始检测”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/status`，目标状态为 `检测中`。

前置状态：

- 维修单必须是 `已开单`。
- 生产设计中，订单必须已指派给当前登录工程师。

系统动作：

- 维修单状态从 `已开单` 变为 `检测中`。
- 机器状态保持或进入 `检测中`。
- 写入机器时间线事件“维修检测中”。
- 写入操作审计 `repair_order:status`。

业务意义：

- 确认机器已经进入检测环节。
- 工程师接单后开始专业检测，订单在工程师个人账号下推进。
- 防止维修单从刚开单直接进入报价、交付或收款。

异常规则：

- 已结单或已作废维修单不能继续推进。
- 非 `已开单` 状态不能再次进入开始检测。
- 未指派给当前工程师的订单，工程师不能检测或修改。

### 3.3 检测报价

入口：

- 前端“检测报价”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/quote`。

前置状态：

- 维修单必须是 `检测中`。

输入：

- `diagnosis`：检测结论，必填。
- `quoted_amount`：客户报价，必须大于或等于 0。
- 生产设计中，工程师应录入机器故障、维修方案和所需 SKU 项目；报价可由 SKU 表自动汇总生成。

系统动作：

- 写入维修单检测结论和报价。
- 保存故障项、维修方案、SKU 明细、成本和客户报价。
- 维修单状态进入 `已报价`。
- 机器状态进入 `已报价`。
- 写入机器时间线事件“检测报价”，内容包含检测结论和报价金额。
- 写入操作审计 `repair_order:quote`。

业务意义：

- 把检测结果和客户报价固化为后续维修、改价、收款、结账的金额基础。
- 把工程师专业判断转化为前台可向客户沟通的清晰报价。
- 让客户确认维修预期，避免维修完成后再产生价格争议。
- SKU 表用于维护常见故障、维修方案、配件/服务价格和成本，减少手工报价随意性。

异常规则：

- 未开始检测不能报价。
- 已作废或已结单维修单不能报价。
- 检测结论不能为空。
- SKU 自动报价后仍允许人工调整，但必须记录调整原因和操作人。

### 3.3.1 前台报价沟通 / 客户确认

入口：

- 前台“待客户确认”订单列表。
- 后端建议提供 `POST /api/repair-orders/{repair_order_id}/confirm-quote`。

操作人：

- 前台。
- 管理员。

前置状态：

- 工程师已完成检测报价。

输入：

- `confirm_result`：客户同意维修、客户拒修、待考虑。
- `confirm_method`：电话、微信、现场、其他。
- `contact_person`：确认联系人。
- `remark`：沟通记录。

系统动作：

- 客户同意维修时，维修单进入可维修状态，处理归属回到原工程师。
- 客户拒修时，维修单进入作废或待取回流程。
- 客户待考虑时，维修单保持待客户确认，不允许工程师直接维修。
- 写入机器时间线事件“报价确认”。
- 写入操作审计 `repair_order:quote_confirm`。

业务意义：

- 把工程师报价和客户决策分开，避免未经客户确认就开始维修。
- 前台作为客户沟通窗口，保留确认方式和沟通痕迹。

### 3.4 改价

入口：

- 机器时间线详情里的改价表单。
- 后端 `POST /api/repair-orders/{repair_order_id}/price`。

输入：

- `quoted_amount`：新报价，必须大于或等于 0。
- `remark`：改价原因或沟通记录。

系统动作：

- 更新维修单报价金额。
- 写入机器时间线事件“维修改价”，内容包含旧报价、新报价和备注。
- 写入操作审计 `repair_order:price`。

业务意义：

- 保留报价调整历史，避免口头改价不可追溯。
- 支持检测加项、客户砍价、配件价格变化等真实业务情况。

当前实现边界：

- 当前代码允许对存在的维修单改价，没有额外限制状态。
- 后续生产规范建议明确：哪些状态允许改价、是否需要管理员或财务复核、已交付后改价是否必须写明原因。
- 生产设计中，工程师追加故障或维修方案导致报价变化时，订单应回到前台沟通客户确认；确认后才能继续维修。

### 3.5 记录维修项目 / 配件

入口：

- 前端“记维修项目”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/items`。

前置状态：

- 当前实现中，维修单必须是 `已报价`。
- 生产设计中，客户确认报价后，原指派工程师可开始维修并记录一个或多个维修项目。

输入：

- `item_name`：维修项目或配件名称，必填。
- `quantity`：数量，默认 1，必须大于或等于 1。
- `cost_amount`：成本金额，必须大于或等于 0。
- `charge_amount`：收费金额，必须大于或等于 0。
- `sku_id`：生产设计中建议关联维修 SKU 表。
- `remark`：项目备注。

系统动作：

- 新增 `repair_items` 明细。
- 维修单状态从 `已报价` 进入 `处理中`。
- 机器状态进入 `维修中`。
- 写入机器时间线事件“维修项目”。
- 写入操作审计 `repair_order:item`。

业务意义：

- 把“客户报价”和“实际维修动作”拆开记录。
- 后续可按维修项目统计成本、收费、利润和责任。
- 工程师负责记录真实维修动作；前台可查看但不直接维护工程师维修明细。

异常规则：

- 未报价不能记录维修项目。
- 未经客户确认的报价不能开始维修。
- 已作废或已结单维修单不能记录维修项目。
- 当前实现中，新增第一个维修项目后维修单进入 `处理中`；已进入 `处理中` 后不能通过同一接口继续追加项目，这是后续需要评估的业务规则。
- 生产设计建议允许工程师在 `处理中` 继续追加多个维修项目；如追加项目影响报价，订单必须回到前台重新确认。

### 3.6 标记待交付

入口：

- 前端“待交付”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/status`，目标状态为 `待交付`。

前置状态：

- 维修单必须是 `处理中`。
- 生产设计中，必须由当前负责工程师操作。

系统动作：

- 维修单状态进入 `待交付`。
- 机器状态进入 `待交付`。
- 写入机器时间线事件“维修待交付”。
- 写入操作审计 `repair_order:status`。

业务意义：

- 表示维修动作已经完成，等待交付检测、客户确认和取机。
- 工程师完成维修后先结清自己的处理责任，再交给前台进行客户取机和收费。
- 把维修执行与交付检测分离，便于质检和客户沟通。

异常规则：

- 未进入维修处理不能标记待交付。
- 已作废或已结单维修单不能标记待交付。

### 3.6.1 工程师结单

入口：

- 工程师个人订单中的“维修完成 / 工程师结单”按钮。

操作人：

- 当前负责工程师。
- 管理员。

前置状态：

- 维修单处于 `处理中` 或 `待交付`。
- 已记录必要的维修项目、故障处理结果和维修备注。

系统动作：

- 将工程师处理状态标记为已完成。
- 订单处理归属转为前台，进入 `待前台收费/交付`。
- 写入机器时间线事件“工程师结单”。
- 写入操作审计 `repair_order:engineer_close`。

业务意义：

- 工程师结单表示维修工作完成，不等于整张订单财务完结。
- 前台据此联系客户取机、解释维修结果并执行收费。

### 3.7 交付检测

入口：

- 前端“交付检测”按钮。
- 后端 `POST /api/repair-orders/{repair_order_id}/deliver`。

前置状态：

- 维修单必须是 `待交付`。

输入：

- `delivery_check`：交付检测结果，必填。
- `remark`：交付备注。

系统动作：

- 写入维修单交付检测结果。
- 维修单状态进入 `已交付`。
- 机器状态进入 `已交付`。
- 写入机器时间线事件“交付检测”。
- 写入操作审计 `repair_order:deliver`。

业务意义：

- 客户取机前确认功能正常。
- 作为财务收款结单前最后一个业务确认节点。
- 前台可查看交付检测结果，用于向客户解释维修完成情况。

异常规则：

- 不能跳过 `待交付` 直接交付。
- 作废单不能交付。
- 结单后不能再次交付。
- 如交付检测不通过，生产设计中应允许退回 `处理中`，归属原工程师继续处理，并写入“质检不通过”时间线。

### 3.8 收款结单

入口：

- 前台“待收费”订单列表。
- 前端“财务流水”页面。
- 维修流程中的“收款结单”按钮会跳转到财务流水并预填 `source_type=repair`、`source_id=repair_order_id`、`direction=收入`。
- 后端 `POST /api/payments`。

操作人：

- 前台。
- 财务。
- 管理员。

前置状态：

- 维修单必须是 `已交付`。
- 生产设计中，工程师必须已结单，订单归属必须已转回前台。

输入：

- `source_type=repair`。
- `source_id=repair_order_id`。
- `direction=收入`。
- `amount`：收款金额，必须大于 0。
- `method`：收款方式。
- `payer`：付款人。
- `payee`：收款人。
- `remark`：备注。

系统动作：

- 创建 `payments` 收入流水。
- 写入机器时间线事件“收入流水”。
- 如果该维修单累计维修收入小于报价金额，则暂不结单。
- 如果累计维修收入达到或超过报价金额，则维修单进入 `已结单`，机器进入 `已结单`，写入关闭时间。
- 写入操作审计 `payment:create`。

业务意义：

- 把维修交付和财务收款分离，符合门店“先交付确认、后收款结单”的闭环。
- 前台负责客户现场收费和订单完结；财务或老板负责后续财务系统核对、报表和批量结账。
- 支持部分收款，直到累计收入达到报价后自动结单。

异常规则：

- 作废维修单不能收款。
- 已结单维修单不能重复收款。
- 未交付维修单不能收款。
- 维修单只能登记收入流水，不能登记支出流水。

### 3.9 作废

入口：

- 后端 `POST /api/repair-orders/{repair_order_id}/status`，目标状态为 `已作废`。
- 前端当前没有显式作废按钮，但后端规则已存在。

允许前置状态：

- `已开单`。
- `检测中`。
- `已报价`。
- `处理中`。
- `待交付`。

系统动作：

- 维修单状态进入 `已作废`。
- 写入机器时间线事件“维修作废”。
- 写入操作审计 `repair_order:status`。

业务意义：

- 客户放弃维修、误开单、无法维修、价格未谈拢等情况应保留单据和历史，而不是直接删除。

后续限制：

- 作废单不能交付。
- 作废单不能收款。
- 作废单不能继续维修。

## 04 维修状态机与异常规则

当前 MVP 标准路径：

```text
已开单 -> 检测中 -> 已报价 -> 处理中 -> 待交付 -> 已交付 -> 已结单
```

生产建议岗位流转路径：

```text
前台简建单 -> 公共订单池/待指派 -> 工程师检测 -> 工程师故障与方案 -> SKU 报价 -> 前台客户确认 -> 工程师维修 -> 工程师结单 -> 前台收费 -> 已结单
```

作废路径：

```text
已开单 / 检测中 / 已报价 / 处理中 / 待交付 -> 已作废
```

岗位待办归属：

| 待办归属 | 触发来源 | 主要处理人 | 允许动作 | 下一归属 |
| --- | --- | --- | --- | --- |
| 公共订单池 / 待指派 | 前台简建单 | 前台、老板、管理员 | 查看、补充接待信息、指派工程师、作废 | 指定工程师 |
| 工程师待检测 | 指派工程师 | 被指派工程师 | 开始检测、记录故障、选择 SKU、生成报价 | 前台待客户确认 |
| 前台待客户确认 | 工程师报价 | 前台 | 联系客户、确认报价、拒修、补充沟通记录 | 原工程师或作废/待取回 |
| 工程师维修中 | 客户同意维修 | 被指派工程师 | 记录维修项目、追加故障、申请改价、维修完成 | 前台待收费/交付 |
| 前台待收费/交付 | 工程师结单 | 前台 | 通知取机、交付说明、现场收费、完结订单 | 已结单 |
| 财务待核对 | 前台收费或客户月结 | 财务、老板 | 核对流水、批量结账、冲正、退款 | 已结单或异常处理 |

状态动作矩阵：

| 维修单状态 | 允许动作 | 禁止动作 | 产生数据 | 机器状态影响 | 财务影响 |
| --- | --- | --- | --- | --- | --- |
| 已开单 | 指派工程师、开始检测、作废 | 报价、记维修项目、交付、收款 | 维修单、开单事件、指派事件 | 机器已进入检测中 | 无 |
| 检测中 | 检测报价、作废 | 未指派工程师操作、记维修项目、待交付、交付、收款 | 检测结论、故障、方案、SKU 报价 | 已报价 | 无 |
| 已报价 | 前台客户确认、改价、作废 | 未确认直接维修、交付、收款 | 报价确认、改价事件 | 维修中或待取回 | 无 |
| 处理中 | 追加维修项目、工程师结单、改价、作废 | 前台代填工程师维修明细、直接收款 | 维修项目、工程师结单事件 | 待交付 | 无 |
| 待交付 | 交付检测、质检不通过退回、改价、作废 | 追加维修项目、收款 | 交付检测 | 已交付或维修中 | 无 |
| 已交付 | 前台收款结单、改价 | 作废、追加维修项目、重复交付 | 收入流水 | 达到金额后已结单 | 维修收入 |
| 已结单 | 查看 | 推进、作废、追加维修项目、重复收款 | 无新增业务数据 | 已结单 | 已闭环 |
| 已作废 | 查看 | 推进、交付、收款 | 无新增业务数据 | 当前实现不额外关闭机器 | 无 |

当前已实现的硬性限制：

- 只能从 `已开单` 进入 `检测中`。
- 只能从 `检测中` 进入 `已报价`。
- 只能从 `已报价` 记录维修项目并进入 `处理中`。
- 只能从 `处理中` 进入 `待交付`。
- 只能从 `待交付` 进入 `已交付`。
- 只能在 `已交付` 后登记维修收入。
- 作废维修单不能交付或收款。
- 已结单维修单不能重复收款。
- 已结单或作废维修单不能继续业务推进。

需要进一步确认或补强的规则：

- 生产设计建议已进入 `处理中` 后允许原工程师追加多个维修项目；影响报价时必须回到前台客户确认。
- 已交付后是否允许改价，以及改价是否需要管理员或财务确认。
- 作废后机器状态是否需要进入单独的“已作废”或“待客户取回”状态。
- 维修单部分收款后是否允许继续改价。
- 客户挂账维修是否统一通过 `payments` 部分收款处理，还是继续支持客户结账单批量结算。
- 工程师可见范围应限制为已指派给自己、且未完成归档的订单；前台可查看全部维修订单。

## 05 回收、库存、销售、财务流

### 回收业务流

标准路径：

```text
回收开单 -> 验机报价 -> 回收改价（可选） -> 付款入库 -> 形成库存 -> 登记回收支出
```

关键动作：

- 回收开单：创建或复用机器档案，创建 `recycle_orders`，机器进入 `检测中`。
- 验机报价：记录检测结果和报价，机器进入 `已报价`。
- 回收改价：记录旧报价、新报价和原因，写入时间线。
- 付款入库：记录实际付款金额，创建 `inventory_items`，机器进入 `回收库存`。
- 回收支出：系统在入库时自动创建 `payments` 支出流水。

业务意义：

- 将回收成本、库存定价、机器档案和客户来源绑定在一起。
- 后续销售时可计算库存成本与收入差额。

### 库存业务流

库存来源：

- 当前新主线库存来自回收业务的付款入库。

库存关键字段：

- `inventory_item_id`：库存项主键。
- `machine_id`：关联机器。
- `recycle_order_id`：关联回收单。
- `status`：库存状态，当前主要为 `可销售`、`已售出`。
- `cost_amount`：回收成本。
- `sale_price`：销售定价。

业务意义：

- 库存不是孤立设备列表，而是机器生命周期中的一个阶段。
- 销售必须从库存项发起，避免重复销售同一台机器。

### 销售业务流

标准路径：

```text
选择库存项 -> 销售开单 -> 库存标记已售出 -> 机器状态已售出 -> 登记销售收入 -> 结单
```

关键动作：

- 销售开单：选择 `inventory_item_id`，录入客户、销售价、销售人和备注。
- 系统创建 `sales_orders`，库存项标记 `已售出`，机器进入 `已售出`。
- 财务登记 `source_type=sale` 的收入流水后，销售单和机器进入 `已结单`。

异常规则：

- 库存项不存在不能销售。
- 已售出库存不能重复销售。

### 财务流水

统一入口：

- 前端“财务流水”页面。
- 后端 `POST /api/payments`。

流水方向：

- 维修：收入。
- 销售：收入。
- 回收：支出。

财务闭环：

- 维修收入累计达到报价后关闭维修单和机器。
- 销售收入登记后关闭销售单和机器。
- 回收支出在回收入库时登记，但不关闭机器，因为机器后续进入库存和销售。

## 06 客户与结账流

客户主数据用于连接维修、回收、销售和财务结账。客户可以在业务开单时创建，也可以通过 `customer_id` 复用已有客户。

客户字段：

- 姓名、电话、微信。
- 客户类型：个人客户、商家客户等。
- 店铺名称、地址。
- 标签、VIP 等级、折扣政策、备注。

客户结账流：

```text
选择客户 -> 查询未结销售和未结维修 -> 选择明细 -> 生成结账单 -> 生成结账明细 -> 更新业务状态
```

当前结账范围：

- 旧主线销售：`devices` 中已售出且 `settlement_status=未结` 的记录。
- 旧主线维修：`repairs` 中 `settlement_status=未结` 的记录。
- 新主线维修：`repair_orders` 中未 `已结单`、未 `已作废` 且报价大于 0 的记录。

结账结果：

- 创建 `settlements`。
- 创建 `settlement_items`，记录来源类型、来源 ID、金额、旧状态、新状态。
- 新主线维修订单结账后进入 `已结单`，关联机器进入 `已结单`。

需要进一步统一的规则：

- 新主线维修既可以通过 `payments` 单独收款结单，也可以通过客户结账批量结算；后续应明确两者关系，避免同一笔维修重复结算。
- 客户结账应优先依赖稳定 `customer_id`，客户姓名只作为搜索展示字段。
- 前台现场收费后，订单可进入已结单或财务待核对状态；财务系统以财务/老板确认的正式流水为准。
- 商家或月结客户可由前台标记挂账，进入客户结账池；散客现结订单不应再进入批量结账池。

## 07 权限、审计与时间线

权限按后端动作控制，不只依赖前端菜单。

维修相关权限：

- 管理员：可创建、更新、读取维修订单，可收款、结账、看报表和审计。
- 前台：可创建维修订单、查看全部维修订单、指派或申请指派工程师、查看工程师报价、记录客户确认、执行客户取机和现场收费；不填写专业检测结论和维修方案。
- 工程师：只能读取和处理已指派给自己的维修订单；可开始检测、记录故障、维护维修方案、选择 SKU、记录维修项目和工程师结单；不能查看未指派订单，不能操作财务结账。
- 员工：如果继续保留通用角色，应拆分或映射到前台、工程师两个岗位，避免一个账号同时拥有全部维修动作。
- 财务：可读取维修订单、创建和读取支付流水、结账、看报表和审计；不能创建或推进维修订单。
- 老板：可查看全部订单、财务、报表和审计，可处理改派、改价审核、退款/冲正、作废争议等高风险动作。

审计层级：

- `machine_events`：业务时间线，面向机器生命周期查看。用于回答“这台机器经历了什么”。
- `machine_notes`：追加备注，面向人工补充说明。用于记录电话沟通、加急要求、特殊情况。
- `operation_logs`：系统操作审计，面向权限和后台追踪。用于回答“谁在什么时候做了哪个后端动作”。

维修时间线应覆盖：

- 机器到店建档。
- 维修开单。
- 指派工程师。
- 开始检测。
- 检测报价。
- SKU 报价明细。
- 前台报价确认。
- 维修改价。
- 维修项目。
- 工程师结单。
- 待交付。
- 交付检测。
- 收入流水。

## 08 当前代码已实现 vs 待补充规则

已实现：

- 机器档案唯一 IMEI 校验和无 IMEI 临时编号。
- 维修单标准状态推进。
- 维修单禁止跳步。
- 作废维修单禁止交付和收款。
- 已结单维修单禁止重复收款。
- 维修收款必须发生在已交付之后。
- 维修收入累计达到报价后自动结单。
- 维修关键动作写入机器时间线。
- 维修相关操作写入操作审计。
- 财务角色可以收款和看报表，员工不能看财务报表。

待补充或待确认：

- 前端缺少维修作废按钮，后端已有作废能力。
- 当前新增维修项目会从 `已报价` 进入 `处理中`，但后续不能继续追加多个项目；如果业务需要多项目维修，需要调整状态规则。
- 改价没有状态限制，生产前需要明确审批和状态边界。
- 新主线维修的单独收款结单与客户批量结账需要统一去重规则。
- 作废后机器状态当前不额外关闭，需要明确机器后续如何处理。
- 维修报表需要进一步区分待检测、待维修、待交付、待收款、已结单、已作废。
- 维修单需要更完整的客户确认记录，例如报价确认时间、确认方式、联系人。
- 生产设计需要补充公共订单池、工程师指派、工程师个人订单列表和订单改派记录。
- 生产设计需要补充 SKU 维修方案/报价表，用于从故障和维修方案自动生成报价。
- 生产设计需要补充前台全量查看、工程师仅看本人订单的后端权限规则。

## 09 测试验收场景清单

维修主流程：

- 新建维修单后，维修单状态为 `已开单`，机器状态为 `检测中`，时间线有“维修开单”。
- 从 `已开单` 点击开始检测后，维修单进入 `检测中`。
- 从 `检测中` 报价后，维修单进入 `已报价`，机器进入 `已报价`，保存检测结论和报价金额。
- 从 `已报价` 记录维修项目后，维修单进入 `处理中`，机器进入 `维修中`，保存项目、数量、成本和收费。
- 从 `处理中` 标记待交付后，维修单和机器进入 `待交付`。
- 从 `待交付` 交付检测后，维修单和机器进入 `已交付`。
- 财务对已交付维修单登记足额收入后，维修单和机器进入 `已结单`。

维修异常流：

- 未开始检测不能报价。
- 未报价不能记录维修项目。
- 未进入待交付不能交付。
- 未交付不能收款。
- 作废维修单不能交付。
- 作废维修单不能收款。
- 已结单维修单不能重复收款。
- 已结单维修单不能追加维修项目。
- 改价必须留下“维修改价”时间线记录。
- 部分收款低于报价时不自动结单，累计达到报价后自动结单。

权限验收：

- 前台可以创建维修单，并在全部维修订单中查看订单状态。
- 前台可以把公共订单池中的订单指派给工程师。
- 前台可以看到工程师报价并记录客户确认结果。
- 前台可以执行客户取机和现场收费。
- 前台不能填写工程师检测结论、维修方案和维修项目。
- 工程师只能看到指派给自己的订单。
- 工程师可以开始检测、记录故障、选择 SKU、生成报价、记录维修项目和工程师结单。
- 工程师不能查看未指派给自己的订单。
- 工程师不能创建维修收款流水或查看经营报表。
- 财务可以读取维修单、创建维修收款流水、查看报表。
- 财务不能创建维修单或推进维修状态。
- 老板/管理员可以完成所有维修、财务、报表、审计、改派和异常审核动作。

跨岗位流转验收：

- 前台简建单后，订单出现在公共订单池。
- 订单指派给工程师后，工程师在自己的账号看到该订单。
- 工程师检测后录入故障和维修方案，并通过 SKU 表生成报价。
- 前台看到报价后联系客户确认，客户同意后订单回到原工程师维修。
- 工程师维修完成并结单后，订单转到前台待收费。
- 前台收费后订单完结，工程师不再继续推进该订单。
- 前台在整个过程中都能查看全部订单进度。

时间线验收：

- 机器时间线能串起建档、维修开单、检测报价、维修项目、交付检测、收款流水。
- 维修项目能在机器详情中看到收费和成本。
- 维修收款能在机器详情中看到金额、方向、方式和时间。
- 手工备注按追加顺序显示，不覆盖旧备注。

回收、库存、销售、财务验收：

- 回收开单后机器进入检测中。
- 验机报价后机器进入已报价。
- 回收入库后生成库存项，机器进入回收库存，并登记回收支出。
- 销售库存项后库存标记已售出，机器进入已售出。
- 销售收款后销售单和机器进入已结单。
- 报表能统计库存数量、机器状态分布和收支方向汇总。
